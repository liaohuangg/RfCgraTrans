# generated by configure / remove this line to disable regeneration
prefix="/root/workspace/RfCgraTrans/build/pluto"
exec_prefix="${prefix}"
bindir="${exec_prefix}/bin"
libdir="/root/workspace/RfCgraTrans/pluto/pet/.libs"
datarootdir="${prefix}/share"
datadir="${datarootdir}"
sysconfdir="${prefix}/etc"
includedir="/root/workspace/RfCgraTrans/pluto/pet/./include"
package="pet"
suffix=""

for option; do case "$option" in --list-all|--name) echo  "pet"
;; --help) pkg-config --help ; echo Buildscript Of "pet Library"
;; --modversion|--version) echo "0.11.4"
;; --requires) echo : ""
;; --libs) echo -L${libdir} "" "-lpet -lisl"
       :
;; --cflags) echo -I${includedir} ""
       :
;; --variable=*) eval echo '$'`echo $option | sed -e 's/.*=//'`
;; --uninstalled) exit 0 
;; *) ;; esac done
